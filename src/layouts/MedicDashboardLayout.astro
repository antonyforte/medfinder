---

---

<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <style>
    :root {
      --sidebar-width: 200px;
      --sidebar-bg: #16403C;
      --primary-color: #07b38d;
      --text-color: #6A8784;
    }

    body {
      margin: 0;
      padding: 0;
      font-family: 'Arial', sans-serif;
      display: flex;
      min-height: 100vh;
    }

    .sidebar {
      width: var(--sidebar-width);
      background-color: var(--sidebar-bg);
      padding: 20px 0;
      display: flex;
      flex-direction: column;
      height: 100vh;
      position: fixed;
    }

    .sidebar-logo {
      padding: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      margin-bottom: 20px;
    }

    .sidebar-logo img {
      max-width: 40px;
    }

    .sidebar-nav {
      display: flex;
      flex-direction: column;
      flex-grow: 1;
    }

    .nav-item {
      padding: 15px 20px;
      color: var(--text-color);
      text-decoration: none;
      font-size: 1rem;
      transition: background-color 0.3s;
    }

    .nav-item:hover, .nav-item.active {
      background-color: #a0a0a033;
      color: var(--primary-color);
      border: 1px solid #07b38d;
    }

    .main-content {
      flex-grow: 1;
      margin-left: var(--sidebar-width);
      padding: 30px;
    }

    .bottom-nav {
      margin-top: auto;
    }

    .emoji-text {
      color: #e91e63;
      margin-top: auto;
      padding: 15px 20px;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="sidebar">
    <div class="sidebar-logo">
      <img src="/logo.svg" alt="Logo">
    </div>
    <nav class="sidebar-nav">
      <a href="/medicDashboard" class={Astro.url.pathname === '/medicDashboard' ? 'nav-item active' : 'nav-item'}>Início</a>
      <a href="/medicDashboard/chats" class={Astro.url.pathname === '/medicDashboard/conversas' ? 'nav-item active' : 'nav-item'}>Conversas</a>
      <a href="/medicDashboard/schedule" class={Astro.url.pathname === '/medicDashboard/schedule' ? 'nav-item active' : 'nav-item'}>Agenda</a>
      <a href="/medicDashboard/medics" class={Astro.url.pathname === '/medicDashboard/medicos' ? 'nav-item active' : 'nav-item'}>Médicos</a>
      <a href="/medicDashboard/configuration" class={Astro.url.pathname === '/medicDashboard/configuracao' ? 'nav-item active' : 'nav-item'}>Configuração</a>
      <a href="/medicDashboard/ajuda" class={Astro.url.pathname === '/medicDashboard/ajuda' ? 'nav-item active' : 'nav-item'}>Ajuda</a>
      
      <div class="emoji-text">
        Substituir texto por emojis ?
      </div>
      
      <div class="bottom-nav">
        <a href="/logout" class="nav-item">Sair</a>
      </div>
    </nav>
  </div>
  
  <main class="main-content">
    <slot />
  </main>
</body>
</html>